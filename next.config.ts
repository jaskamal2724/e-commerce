import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "imgs.search.brave.com",
      "demonslayermerchandise.shop",
      "www.pexels.com",
      "images.unsplash.com",
      "m.media-amazon.com"
    ], // Add your domains here
  },
};

export default nextConfig;
